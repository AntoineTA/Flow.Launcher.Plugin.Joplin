name: Publish Release

on:
  workflow_dispatch:
  push:
    tags:
      - "v*"

jobs:
  publish:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 8.0.x

      - name: Build
        run: |
          dotnet build Flow.Launcher.Plugin.Joplin.csproj -c Release

      - name: Package
        run: |
          $version = "${{ github.ref_name }}".TrimStart('v')
          Compress-Archive -Path "bin/Release/*" -DestinationPath "Flow.Launcher.Plugin.Joplin-$version.zip"

      - name: Create Release
        uses: softprops/action-gh-release@v1
        with:
          files: Flow.Launcher.Plugin.Joplin-*.zip
          generate_release_notes: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
